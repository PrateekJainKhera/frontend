import { Product } from '@/types'

// Empty mock products array - using real backend API instead
// Kept for backward compatibility with other mock data files
export const mockProducts: Product[] = []
